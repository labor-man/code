列出shell历史
history

环境变量HISTSIZE控制保留的命令数量
HISTSIZE

重复历史中编号为N的命令
!N

重复上一个命令
!!

重复最近包含string的命令
!string

上一个命令的第N部分
!:N

上一个命令的第一个参数
!^
上一个命令的最后一个参数
!$

查询shell命令历史
Ctrl-r	反向查询历史
Enter 	执行命令
Arrow 	修改命令
Ctrl-g 	取消查询

tab自动补全
	命令
	文件, 文件夹, 路经
	环境变量
	用户名(~)
创建别名

git config alias.last 'log -1 --stat'
这会使git last等价于git log -1 --stat

git config alias.r reset
这会使git r <file> 等同于 git reset <file>

-------------------------------------------------

涉及外部命令
git config --global alias.p '!pwd'
将git p 定义为 pwd 的别名
忽略文件-------------------------------------------------

https://github.com/github/gitignore
针对多种语言的.gitignore文件

查看-------------------------------------------------

查看文件状态（Untracked Unmodified Modified Staged）
git status

查看简要信息（输出中左栏是暂存区状态，右栏是工作区状态）
git status -s

将内容添加到下一次提交Staged-------------------------------------------------

git add

查看更改-------------------------------------------------

查看未暂存的更改
git diff

查看要添加到下次提交的更改
git diff --staged

使用工具比较查看（默认可能是vimdiff）
git difftool

提交-------------------------------------------------

提交
git commit

将所有跟踪文件暂存并且提交
git commit -a

移除-------------------------------------------------

从仓库中移除（但是保留文件，此时为未跟踪状态）
git rm --cached <file>

从跟踪清单移除并删除文件
git rm <file>

移除已经暂存的
git rm <file> -f

移动-------------------------------------------------

移动文件（或重命名）
git mv README.md README
